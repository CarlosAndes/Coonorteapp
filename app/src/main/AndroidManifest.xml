<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoonorteApp"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 🚀 Pantalla PRINCIPAL: Login -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- 🟩 Pantalla de REGISTRO -->
        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="false" />

        <!-- 🟩 Nueva PANTALLA de MENÚ -->
        <activity
            android:name=".ui.menu.MainMenuActivity"
            android:exported="false" />

        <!-- 🟩 Pantalla de ENCOMIENDAS -->
        <activity
            android:name=".ui.encomiendas.EncomiendasActivity"
            android:exported="false" />

        <!-- 🟩 Pantalla de QUEJAS -->
        <activity
            android:name=".ui.quejas.QuejasActivity"
            android:exported="false" />

        <!-- 🟩 🚀 PANTALLA DE COMPRAR TIQUETE (VENTAS) — ESTA FALTABA -->
        <activity
            android:name=".ui.ventas.VentasActivity"
            android:exported="false" />

    </application>

</manifest>
